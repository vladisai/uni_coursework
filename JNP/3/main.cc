#include <bits/stdc++.h>

#include "point.h"

#define F first
#define S second
#define pb push_back
#define INF (1 << 30)
#define SQR(a) ((a) * (a))

using namespace std;

const int N = 1111;

int main()
{
    Point p(10, 10);
    cout << p.x() << endl;    
    return 0;
}
