#include <bits/stdc++.h>

#include "strdeque.h"

#define F first
#define S second
#define pb push_back
#define INF (1 << 30)
#define SQR(a) ((a) * (a))

using namespace std;

const int N = 1111;

int main()
{
    append(3);
    cout << getLast() << endl;


    fflush(stdout);
    fflush(stderr);
    fprintf(stderr, "\n\n Execution time: %.4fs", double(clock()) / CLOCKS_PER_SEC);

    return 0;
}
